import React from "react";

const Jumbotron = () => (
  <div className="jumbotron">
    <h2>Click-Me Game</h2>
    <p>
      Click an image to add points, but be sure to not click an image twice.
    </p>
  </div>
);

export default Jumbotron;
